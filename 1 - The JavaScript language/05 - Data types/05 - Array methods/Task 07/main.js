// Task 1.5.5.7

/**
 * @param {Array} arr Array of objects with 'name' property
 * @returns {Array} an Array of all the names of all the objects in the same 
 *      order as given.
 */
function mapToNames(arr) {
    return arr.map( item => item.name );
}

